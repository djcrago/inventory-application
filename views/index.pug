extends layout

block content
  h2= title

  p Shown below are basic statistics dynamically generated by accessing the database 

  p #[strong Total Number of Items:] !{item_count_total}
  p #[strong Total Number of Categories:] !{category_count_total}

  h3 Categories
    
  ul.home-categories
    each category_count in category_counts
      li
        a(href="/inventory/category/category_count.name") #{category_count.name}
        |: #{category_count.count} items
