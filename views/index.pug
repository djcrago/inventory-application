extends layout

block content
  h2= title

  p Shown below are basic statistics dynamically generated by accessing a database 

  p #[strong Total Number of Items:] !{all_items}
  p #[strong Total Number of Categories:] !{all_categories.length}

  h3 Categories
    
  ul
    for category in all_categories
      li.m-l-16
        a(href=`/inventory/category/${category.name}`) #{category.name}
        |: #{category.item_count} items
